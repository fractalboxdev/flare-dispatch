// The substrate's version, reported on /health (specs/platform.md § Tenancy
// and deploy: patch distribution needs the floor's version observable — a
// security release declares a minimum supported version against this).
// Kept in lockstep with apps/substrate/package.json by review.
export const SUBSTRATE_VERSION = "0.1.0";
