{
  "name": "@fractalboxdev/flare-dispatch-github-app",
  "version": "0.0.0",
  "private": true,
  "description": "FlareDispatch GitHub App auth — RS256 App JWT signing, installation-token exchange + in-memory cache, and check-runs create/update.",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing.ts",
    "./registration": "./src/registration.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "msw": "^2.7.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.0"
  }
}
