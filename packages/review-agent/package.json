{
  "name": "@fractalbox/flare-dispatch-review-agent",
  "version": "0.0.0",
  "description": "Provider-agnostic, Worker-side code-review engine that calls a model through the `modelGateway` capability (Workers AI binding via an AI Gateway — no model API key). Powers the pr-review run.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fractalbox/flare-dispatch-core": "workspace:*",
    "effect": "^3.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "typescript": "^5.6.3",
    "vitest": "^3.2.0"
  }
}
