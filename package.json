{
  "name": "flare-dispatch",
  "version": "0.0.1",
  "private": true,
  "description": "Offload CI and agentic workflows from GitHub Actions to Cloudflare.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "pnpm -r --filter \"./packages/*\" --filter \"./apps/dispatcher\" --filter \"./apps/substrate\" --filter \"./runs\" exec tsc --noEmit",
    "docs:reference": "typedoc --options apps/docs/typedoc.json",
    "lint": "oxlint",
    "test": "vitest run --passWithNoTests",
    "format": "oxfmt --write"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250510.0",
    "@types/node": "^22.19.0",
    "effect": "^3.21.0",
    "oxfmt": "^0.62.0",
    "oxlint": "^1.71.0",
    "typedoc": "^0.28.20",
    "typedoc-plugin-markdown": "^4.12.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.0",
    "wrangler": "^4.50.0"
  },
  "engines": {
    "node": ">=22.13"
  },
  "packageManager": "pnpm@11.1.3"
}
