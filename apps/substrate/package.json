{
  "name": "@fractalboxdev/flare-dispatch-substrate",
  "version": "0.1.0",
  "private": true,
  "description": "The execution substrate: containers under deny-all egress, ticket-gated admission, and the service-binding facade consumers ride.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:workers": "vitest run --config vitest.workers.config.ts",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.12.4",
    "@fractalboxdev/flare-dispatch-substrate-contract": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.21",
    "@cloudflare/workers-types": "^4.20260518.1",
    "@types/node": "^22.19.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.0",
    "wrangler": "^4.50.0"
  }
}
